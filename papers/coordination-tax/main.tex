% main.tex - the coordination-tax paper. Hand-written; gen-tex.mjs leaves it alone.
% Journal-manuscript format: single-column article with author-year (natbib) citations,
% suitable as an initial submission to an astrobiology journal (e.g. the International
% Journal of Astrobiology, the venue of Nicholson & Forgan 2013, which this paper extends).
% Deliberately uses only TeX Live packages - no bundled publisher class, no biber - so CI
% typesets it unchanged; the publisher's own house template is applied at submission time.
% Bibliography is generated: ../refs.bib comes from frontend/src/sources.ts.
% Figures are generated (gitignored vector PDFs) by the swarm module via
% `uv run --extra dev python -m experiments.paper_figures`.
\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage[round,authoryear]{natbib}
\usepackage{url}
\bibliographystyle{plainnat}

\title{The Coordination Cost of Light-Speed Delay in a\\ Self-Replicating Probe Swarm Scales as $v/c$}
\author{Noah Hyd\'en\\[2pt]
  \normalsize Independent researcher\\
  \normalsize ORCID: 0009-0003-4523-0467}
\date{}

\begin{document}
\maketitle

% Reserved Zenodo DOI, printed on the title page when the zenodo workflow has
% generated doi.tex (gitignored, issue #8). Absent in ordinary builds, so nothing
% prints; the paper typesets identically with or without a minted DOI.
\IfFileExists{doi.tex}{\input{doi.tex}}{}

\begin{abstract}
A swarm of self-replicating probes fills a stellar field star by star. The standard
exploration-timescale model grants every probe perfect, instantaneous knowledge of which stars are
already settled, and names finite light-speed as future work, which we add and measure. First, a
caution: a coarse timestep batches many launches into one stale snapshot, and the collisions appear
to slow the fill by tens of percent -- a discretization artifact that vanishes once each probe
decides at its true arrival time, leaving a few percent at directed-energy speeds, nothing at
slingshot speeds, and the slingshot speed-up intact. The dominant cost is redundant travel: the two
regimes build essentially the same number of probes, but stale views send them to already-claimed
stars. A short collision argument, confirmed in simulation, shows the delay multiplies wasted
journeys by at most 1 + v/c, so the coordination tax -- extra wasted trips relative to a perfect-information
swarm -- scales linearly with the probe's speed in units of light, v/c, with a measured coefficient
below that derived ceiling of one (saturation removes waste the delay would otherwise add) that
itself shrinks with system size: about 0.8 v/c on a few-thousand-star field -- a sixth at
directed-energy speeds -- but only about 0.07 v/c on our quarter-million-star headline field, a fuel
tax of about 1.3% at directed-energy speeds. That baseline is itself wasteful -- about nine in ten
journeys are redundant even with a perfect map -- so this is a v/c surcharge on an already-saturated
process. The fuel tax falls with system size, from about a fifth at a few hundred stars to one part
in eighty at a quarter million over a 1024-fold range -- a genuine bulk decline that survives both an
interior-only and a periodic-box control -- while a secondary fill-time cost does the opposite:
negligible beside the fuel tax on small fields, it comes to exceed it at scale (about 10% against
1.3% at directed-energy speed), set by the front's light-lag across the field rather than by wasted
trips. The tax grows with the branching factor, holds under a clumpy field, and is no hard floor: a
probe that listens in flight recovers essentially all of the wasted-arrival tax, at a fill-time cost
of its own. We denominate the tax in journeys, since whether the waste also costs energy is
mission-dependent.
\end{abstract}

\noindent\textbf{Keywords:} self-replication; interstellar exploration; light-speed delay;
multi-agent coordination; swarm dynamics

\bigskip

\input{body/introduction}
\input{body/the-light-delayed-belief-model}
\input{body/the-coordination-tax}
\input{body/discussion}
\input{body/conclusion}

\bibliography{../refs}

\end{document}
