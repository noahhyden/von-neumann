"""Reproducibility harness for the spine paper (FINDINGS #9).

Not part of the installed `spine` package - it is a driver that runs the pure `spine`
fold, writes committed JSON under `results/`, and is welded to the fold by
`tests/test_measure_results.py`. See `measure.py`.
"""
